# RPG 章节制作手册

> 来源：第一章《空缺者／别西卜驱魔战役》的完整制作与终审经验。
> 用途：后续章节立项、剧情、关卡、代码、UI、配置、测试与发布的统一参考。
> 原则：先让玩家理解因果，再增加内容；先建立可验证契约，再扩大系统。

## 一、先定义章节交付物

一个可发布章节不是一组任务函数，而是同时闭合以下八条链：

1. **叙事链**：玩家为何来到这里，发现、误判和修正了什么，最后付出什么代价。
2. **调查链**：观察事实 → 提出竞争假说 → 交叉验证 → 阶段复盘。
3. **关卡链**：探索、战斗、解谜、准备、Boss、裁决、尾声之间有明确节奏变化。
4. **角色链**：引导者、情感见证人、制度代表和敌对意志持续回应玩家行动。
5. **玩法链**：章节调用既有职业、圣物、仪式、恶魔生态与奖励系统，而不是另造孤岛。
6. **状态链**：开始、加入、锁队、失败恢复、重演、结算、清理均有唯一且可恢复的状态。
7. **美术链**：Bossbar、Title、Tellraw、场景标签、物品文本和粒子遵守同一视觉语法。
8. **发布链**：源码、生成包、压缩包、游戏实例、图鉴、工程文档与 GitHub 是同一版本。

立项时先写一页“章节契约”，包含标准时长、核心冲突、玩家最终应能复述的一句话、状态阶段、关键系统和验收标准。没有这页，不开始堆函数。

## 二、剧情与解谜：答案不能由单个发光点直接给出

### 1. 楔子的最低职责

楔子应在 20–60 秒内交代：

- 玩家以什么身份进入事件；
- 正常世界出现了哪两个互相冲突的事实；
- 玩家为什么不能离开；
- 本章采用什么调查方法。

第一章有效的结构是“粮册满仓／街边空碗”“墓地增员／死者回家”。它先制造矛盾，不急着说出“别西卜”。后续章节也应先让玩家看见世界失常，再给专有名词。

### 2. 每轮调查使用四步循环

| 步骤 | 玩家获得什么 | 禁止事项 |
|---|---|---|
| 观察 | 可直接感知的事实 | 直接写出 Boss 名、幕后真相或标准答案 |
| 假说 | 至少两个暂时合理的解释 | 只提供一个明显正确选项 |
| 验证 | 来自另一人物、地点或系统的证据 | 用同一物证自证结论 |
| 复盘 | 已知、矛盾、下一步 | 与下一阶段对白同刻刷屏 |

线索设计应满足“一证排除一错解，多证才能形成结论”。第一章用火灾、疫病、盗粮三个错误解释衬托“制度清洗＋暴食寄生”的复合真相；这个方法比增加密码锁更能形成推理感。

### 3. 误导必须公平

好的误导不是隐藏信息，而是让现有事实暂时支持错误解释。纠正时必须指出旧解释解释不了的矛盾。至少安排两层：

- **现象误导**：把超自然事件误认成灾害、事故或普通怪物。
- **责任误导**：把执行者误认成全部源头，之后区分个人、制度与恶魔意志。

### 4. 让玩家随时找回剧情

- 玩家面板提供随 Stage 更新的“案件梗概”。
- 每个调查段末显示“已知／矛盾／下一步”。
- 复盘至少独占 200 tick（10 秒），再进入下一阶段。
- Boss 入场前重复对象、弱点、工具与风险，但不重复完整历史。
- 章末用一句话闭合全部因果，并给出下一章钩子。

## 三、对话与角色：让信息拥有说话者

同一段信息由不同角色说出，会改变玩家对它的判断：

- **引导者**负责方法与谨慎，不替玩家下最终结论。
- **见证人**提供生活细节、情感代价和制度无法抹平的具体姓名。
- **制度代表**使用准确、冷静、合法化的措辞制造压力。
- **Boss**不做百科讲解，只暴露欲望、世界观和对玩家选择的诱惑。

对话密度遵守：一刻最多一个核心说话者；战斗中使用短句；解释性对白放在安全窗口；连续三句以上必须分时。判断章节是否讲清楚，不看文档字数，而看首玩玩家能否说出“谁做了什么、为什么、我为何继续”。

## 四、关卡节奏：30–60 分钟不等于强制等待

推荐节奏：

```text
楔子 → 低压观察 → 首次异常 → 护送/罪仆战
     → 多路线调查 → 真名假说 → 战前整备
     → Boss与仪式 → 裁决 → 代价与职业强化
```

节奏变化来自任务性质变化，不来自长读条。调查读条用于确认玩家确实停留观察，通常 2–4 秒；复盘窗口用于阅读，通常 10 秒；更长时长应由可选路线、战斗策略和环境叙事产生。

第一章扩写后的有效组合是“调查 → 主动判断 → 错误具象成短战斗 → 安全复盘”。排序、排除和器具配对使用不同认知动作；错误不会清空整幕，因而增加游玩时间而不制造重复劳动。关键对白必须在敌人生成前、波次清空后或 Boss 锁血窗口播放，战斗中只输出机制提示。

## 五、状态机与多人安全

### 1. 章节实例必须有唯一所有权

控制器、成员、场景点、罪仆、Boss、法阵和临时展示实体都写入同一章节 ID。所有跨实体命令同时检查标签与 ID；距离只能缩小搜索范围，不能代替所有权。

### 2. 队伍在写标签前校验

正确顺序：实例存在 → 加入阶段 → 人数上限 → 写成员标签与 ID／会话 ID → 增加 roster。不要先加入再钳制人数分数，否则多余玩家仍获得成员权限。

### 3. 永久进度必须幂等

奖励、经验、成就和下一章权限都以个人领取标记保护。失败恢复只恢复当前阶段资源，不撤销已完成调查；清理按章节 ID 执行，不扫描或删除附近无关实体。

## 六、统一配置：配置存在不等于配置生效

### 1. 配置只暴露真正可调的内容

应开放：

- 物品本体、模型、身份组件、发放入口；
- NPC、Boss、罪仆的实体类型、显示名、召唤函数和生命；
- 罪仆波次与职责文案；
- 全部本地生成位置；
- 队伍规模、半径、读条、复盘、恢复与场地净空；
- 章节色板和 Display 参数；
- 调试入口。

应固定并明确写成协议：章节 ID、Boss 身份、领主编号、能力路由角色编号、控制器类型、所有权标签、安全权限。不要在 JSON 中保留只用于注释、改了却不影响产物的字段；确有需要时用 `position_only`、`generated` 等明确语义区分。

### 2. 每个字段都要有“消费者证据”

配置字段应满足至少一种验收：

- 生成函数中由该字段构造命令；
- 生成清单记录其摘要；
- 严格检查器读取产物并验证对应值；
- 变异测试修改字段后，产物出现预期差异。

第一章暴露过的典型假接线包括：只在错误提示中显示边界、人数只钳分数不拒绝成员、物品入口配置后仍调用固定函数、色板配置后代码仍写死 Hex、波次字段存在但函数仍固定召唤名单。后续章节应在首轮实现时就搜索这些模式。

### 3. 坐标统一使用章节原点

所有位置使用本地坐标 `^ ^ ^`，由玩家起点和吸附后的四向朝向决定。玩法标记与美术道具必须读取同一坐标源。安全平面采样数组是实际检查范围，`headroom` 必须逐层生成空气检测，不能只出现在提示文字中。

## 七、UI 与文本规范

### 1. 信息层级

- Bossbar：章节目标与量化进度。
- Title/Subtitle：章节、Boss、裁决等少量高价值转折。
- Tellraw：人物对白、观察、复盘和点击选择。
- Actionbar：统一 HUD 合成的战斗技能与玩家状态，不承载章节剧情。

### 2. 色彩与字重

颜色全部来自章节色板。前缀可加粗，本名和后缀默认不加粗；人物名用阵营色，正文保持灰白；同一语义在 Bossbar、Tellraw、物品和图鉴中使用同一颜色。禁止在生成器中散落与配置色板相同的 Hex 字面量。

### 3. 持续 UI 会覆盖一次性 UI

每 tick 写入的 Bossbar/Title 状态必须与阶段 Enter 文案一致。第一章曾出现 Enter 写“楔子”，持续 UI 又覆盖成“序幕”的问题。验收时必须同时检查 Enter、持续 UI 和最终屏幕，而不是只看生成器的一处字符串。

### 4. 专职 UI 复核

每章保留独立 UI agent，只负责层级、颜色、字重、可读时序、既有数据包风格和屏幕冲突。UI agent 不改核心玩法；其结论由严格检查器固化，避免下次回归。

## 八、调试工具：便利不能污染正式存档

每章至少提供调试菜单、以当前位置创建实例、发放全部物品、召唤 Boss、召唤全部章节生物、列出语义坐标和跳转每个 Stage。

阶段跳转先按章节 ID 清理临时对象，再添加 `debug.no_commit`。最终阶段的正式结算、奖励、经验、成就与下一章权限必须显式使用 `unless debug.no_commit` 门控。不要只相信“调试函数本身没写奖励”，因为正式 tick 可能在下一刻自动结算。

## 九、测试与独立终审

自动检查至少覆盖：

1. 数据包语法、资源位置和版本字段；
2. 章节状态机、成员/ID/会话隔离；
3. Boss、罪仆、仪式和恢复链；
4. 配置摘要、全部入口文件与关键字段接线；
5. UI 组件、字重、颜色和 Actionbar 所有权；
6. 楔子、对白密度、竞争假说、交叉验证、复盘停留和章末梗概；
7. 资源包模型、字体与纹理引用；
8. 每 tick 性能预算。

自动检查通过后，再启用独立 checker agent 做只读审计。它应主动寻找“检查器没覆盖但人能看出的契约漏洞”，尤其是同 tick 时序、间接正式结算、配置字段未消费和发布包过期。修复后必须让同一 checker 再跑，直到 PASS。

## 十、发布流程：源码通过不代表实例已更新

严格顺序：

1. 完整构建；
2. 图鉴/文档构建；
3. 数据包、资源包与叙事/UI 检查；
4. `git diff --check` 与脚本编译；
5. 备份当前实例 DP zip、展开 DP 和 RP zip；
6. 重新 package；
7. install 到所有现有世界与资源包目录；
8. 比较工作区 zip 与实例 zip 的 SHA-256；
9. 比较工作区与所有展开世界的关键函数、配置清单哈希；
10. 打开 zip 验证配置清单和关键修复确实在归档内；
11. 独立发布终审；
12. 提交并推送 GitHub。

第一章最后一个阻断就是“展开源码已修复，但 zip 和实例仍是旧版”。因此发布验收必须验证产物内容，不能以 package 命令打印 `installed` 作为唯一证据。

## 十一、下一章开工模板

### 立项

- [ ] 玩家最终能复述的一句话已写定。
- [ ] 楔子建立身份、矛盾、动机与调查方法。
- [ ] 至少两种竞争假说与两层公平误导。
- [ ] 30–60 分钟节奏表和可选探索预算。
- [ ] 章节复用的既有系统已列出。

### 实现

- [ ] 单一配置源只包含真正可调字段。
- [ ] 所有物品、生物、Boss 与位置由配置消费。
- [ ] 全部阶段有失败恢复、重演和清理路径。
- [ ] 多人加入顺序、固定 roster、ID 与会话隔离正确。
- [ ] 复盘有独立阅读窗口。
- [ ] UI 使用章节色板，无散落硬编码。
- [ ] 调试菜单与 `debug.no_commit` 完整。

### 验收与发布

- [ ] 核心、配置、UI、叙事、资源包、性能检查全部通过。
- [ ] UI agent 已复核屏幕层级与风格。
- [ ] checker agent 已完成独立只读终审。
- [ ] 实例已备份。
- [ ] zip、展开包与工作区哈希一致。
- [ ] 图鉴、工程文档、游戏内书与 GitHub 同步。

## 十二、第一章最值得保留的经验

章节丰富度不来自更多按钮、更多线索名或更密集的特效，而来自玩家在行动中不断修正理解：先相信一个合理解释，再亲手发现它解释不了的东西；先把某人视为敌人，再看见他背后的制度；最后发现胜利并没有结束问题，却改变了玩家在世界中的身份。代码、UI、配置和测试的职责，是让这条理解变化可靠、清楚、可重演地发生。
