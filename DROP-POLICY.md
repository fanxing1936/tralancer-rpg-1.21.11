# 生物死亡掉落策略

适用版本：Minecraft Java Edition 1.21.11。

## 玩家可见规则

- 普通生物死亡时不再产生通常实体表中的原版战利品；下文列明少数表外边界。
- 数据包明确编写的掉落仍然生效，包括七张现有实体表、35 位罪仆的专属表、Boss 裁决和章节奖励。
- 充能苦力怕击杀生物不再额外生成头颅。
- 生物所持普通装备不会作为随机死亡掉落；抢夺附魔也不能绕过此限制。
- 佣兵死亡时，玩家交给他的主手武器通过 `rpg:squad/equipment_return` 原样返还。

以下内容完全不在本规则处理范围内：

- 玩家死亡后的背包物品；
- 玩家主动按丢弃键扔出的物品；
- 挖掘方块、打开容器、钓鱼和考古所得物；
- 数据包主动执行 `give`、`loot give` 或 `loot spawn` 发放的奖励；
- 仪式、锻造和佣兵系统主动生成的物品实体。

实现中没有使用 `doMobLoot false`，也没有扫描或清除地面的普通物品实体。

## 保留的实体掉落表

Minecraft 命名空间下现有七张数据包定制表保持原样：

`creeper`、`husk`、`skeleton`、`skeleton_horse`、`zombie`、`zombie_horse`、`zombie_villager`。

35 位罪仆通过实体自身的 `DeathLootTable: "rpg:minion/..."` 使用各自专属表，不会被原版表覆盖影响。无尽模式游柱继续使用数据包指定的空表。

`player`、`armor_stand` 和 `mannequin` 不在普通生物清空名单中，防止误伤玩家死亡背包或展示用装备。

## 技术边界

纯数据包且不关闭全局 `doMobLoot` 时，Minecraft 1.21.11 仍有少量死亡物品绕过通常的实体掉落表：

1. 凋灵的下界之星由 `WitherBoss.dropCustomDeathLoot` 直接生成；原版 `entities/wither` 表本身就是空表。当前策略不会拦截下界之星。
2. 马、驴、骡、羊驼等坐骑的内部物品栏，以及带箱坐骑的箱子物品，由实体的 `dropEquipment` 直接抛出。当前策略不会吞掉玩家存放在坐骑中的物品。
3. 悦灵与猪灵的内部库存、悦灵受托主手、铜傀儡后续托管的物品，也有专门的物品返还代码，不被当成普通随机战利品吞掉。
4. 末影人搬运的方块使用方块掉落表生成。因为本规则不修改方块表，所以这类携带物仍可返还。

这些边界被有意保留：要在纯数据包中彻底拦截，通常需要临死前清空实体库存、全局关闭生物掉落，或事后删除无法可靠辨明来源的地面物品。三种做法都会造成玩家物资损失或破坏数据包自定义掉落，因此没有采用。

充能苦力怕头颅不属于上述边界。1.21.11 已将其放入 `minecraft:charged_creeper/*` 表，本策略会安全地将整组表覆盖为空。

生物在首次被全局 tick 检测到时登记装备规则，并关闭自动拾取，防止拾取行为把普通装备掉落概率改回必掉。佣兵的显式交接武器功能不受影响。管理命令在同一刻中“新召唤后立即击杀”的极端链路可能早于登记，不作为普通装备规则的保证范围。

## 已完成的隔离验证

在独立 1.21.11 服务端中确认：

- 佣兵死亡只返还一份主手武器，保留附魔、耐久值和自定义数据；
- 原为必掉 `2f` 的普通卫道士武器在登记后不掉落；
- 原有自定义 `DeathLootTable` 没有被普通生物规则覆盖；
- 已存在的普通地面物品数量和数据不变；
- 凋灵星、驴的库存及箱子确实继续返还，与上述技术边界一致。

## 构建与验证

生成：

```text
python _tools/add_drop_policy.py rpg --source-jar _endless_server_qa_20260827/versions/1.21.11/server-1.21.11.jar
```

`--source-jar` 用于校验生成器内置的 1.21.11 原版实体表清单；正式构建在已经锁定版本时可省略。

检查：

```text
python _tools/check_drop_policy.py rpg
```

服务端验收至少应覆盖：普通僵尸、普通卫道士、抢夺武器击杀、装备武器的怪物、充能苦力怕头颅、35 位罪仆、佣兵受托武器返还、玩家手动丢弃物和玩家死亡背包。
