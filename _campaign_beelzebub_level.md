# 第一章《空缺者》——别西卜驱魔战役关卡落地规格

> 文档性质：主战役生成器的关卡／场景实现源，不是剧情改写稿。  
> 剧情权威：`BEELZEBUB-CAMPAIGN-CHAPTER-1.md`；世界观权威：`LORE.md`。  
> 目标版本：Minecraft Java 1.21.11，数据包命名空间 `rpg`。  
> 目标时长：首次单人 45–55 分钟；熟练玩家 30–40 分钟；完整探索不超过 60 分钟。  
> 核心原则：只生成实体、粒子、声音、文本和记分板状态；绝不放置、替换或删除世界方块。

---

## 1. 关卡必须兑现的剧情条件

以下是实现验收条件，不允许为了复用通用系统而删减：

1. 玩家先看见“空缺者”的社会异常，再学会用圣器辨认空壳；不能用三个发光文字点代替活的空缺者。
2. 五名罪仆必须按“先锋＋猎手／司祭＋咒使／处刑者”三轮形成职责关系，不能五只一次性散放。
3. 环境调查只能形成“别西卜真名假说”和弱点结论；正式真名必须由 Boss 战中三种不同招式见证完成。
4. Boss 固定 700 生命，420 生命处锁血，之后完整复用镇魔、固阵、裁决四阶段系统。
5. 别西卜最终必定逃脱；四种裁决都保留独立选择和演出，但必须截断通用完整核心、罪器、封印遗物和真实契约奖励。
6. 米拉必须是可被玩家保护、失守后可救回、尾声由玩家主动释放魔力救下的真实角色，而不是一次性旁白。
7. 场景能在玩家主动选择的安全平面展开；验证失败时不留下任何场景实体，也不改动地形。
8. 单人、2–4 人队伍、成员掉线重连、团灭、Boss 异常消失、仪式崩溃和重演都要有明确恢复路径。

---

## 2. 空间模型：一块平面演完一座后方城市

### 2.1 总占地

以章节锚点 `rpg.ch1.anchor` 为原点，锚点面朝剧情推进方向。全部坐标均使用锚点局部坐标 `^x ^y ^z`：

- 主场景占地：`x = -18..18`，`z = -6..58`。
- 最低净空：地面上方 4 格全空；Boss 圆心上方 6 格全空。
- 暂停／成员检测半径：96 格。
- 所有章节查询、清理、生成都必须以锚点为执行位置并限制 `distance=..72`；禁止用不带距离的全局 `@e[tag=rpg.ch1.*]` 操作剧情实体。

这是一个“舞台式城市切片”：同一块平面会随幕次清走旧陈设并换成后街、封锁线、粮仓与裁决现场。它不宣称真的生成整座城，因此不需要复制建筑，也不会覆盖玩家作品。

### 2.2 朝向与网格

启动时把玩家朝向吸附到最近的四个正交方向，再创建锚点。不要保留任意角度，否则局部坐标会落在方块对角线上，地形探针与实体脚底无法一致。

建议接口：

- `rpg:campaign/beelzebub/scene/snap_rotation`
- 临时计分板 `rpg_ch1_yaw` 存储 `Rotation[0]`。
- 四个结果：`0f`、`90f`、`180f`、`-90f`；锚点俯仰统一 `0f`。

### 2.3 场景分区

| 区域 | 局部坐标 | 主要用途 | 同时存在的战斗实体上限 |
|---|---|---|---:|
| 封锁线／简报点 | `z=-4..3` | 序幕、队伍锁定、失败返回点 | 0 |
| 后街餐桌 | `x=2..9,z=5..11` | 活空缺者、母亲与孩子 | 0–2 空壳碎片（仅误操作） |
| 档案摊位 | `x=-10..-3,z=8..14` | 明日死亡名册、借用圣器 | 0 |
| 钟灰与慈济所后门 | `x=-3..4,z=14..19` | 米拉登场、罪仆转场 | 0 |
| 街区战斗场 | `x=-14..14,z=18..34` | 三轮罪仆战、米拉护卫 | 2（第三轮 1） |
| 腐蝇追踪路 | `x=-7..7,z=31..48` | 顺序追踪、确认活动区域 | 0 |
| 第七粮仓／Boss 场 | 圆心 `^0 ^ ^44`，半径 14 | 环境罪证、物资、700 HP Boss、四阶段仪式 | Boss 1＋通用召唤 1–3 |
| 尾声登记点 | `x=-8..8,z=4..16` | 米拉救援、塞维拉、职业选择 | 4 剧情演员 |

粮仓不是实体墙体迷宫。用少量 `block_display`／`item_display` 构成“封条、货架、餐盘、账簿、通风井”的轮廓，竞技空间始终开放，避免显示实体拥有碰撞的假象，也避免 Boss 路径被玩家原有地形卡死。

---

## 3. 启动前安全校验

### 3.1 启动门禁

`rpg:campaign/beelzebub/start` 在创建正式锚点之前必须依次通过：

1. 当前维度为 `minecraft:overworld`。
2. 世界中不存在其他 `rpg.ch1.anchor`；第一版只支持一个共享章节实例。
3. 玩家未在旁观模式，且脚下站在完整方块表面。
4. 以吸附后的朝向检查整个 `37 × 65` 场地已加载。
5. 场地采样点地面非空气／流体／危险方块，脚部至头顶至少 3 格空气；Boss 九点采样额外要求 5 格空气。
6. 72 格内不存在村民、铁傀儡、袭击队、现存恶魔领主、活动仪式锚或其他章节罪仆，防止剧情空缺者污染玩家村庄或与别的战斗串台。
7. 启动玩家明确收到提示：本章不修改地形，但会在该平面生成最多约 35 个临时实体，并占用 30–60 分钟。

### 3.2 采样网格

一次性启动校验可以承受较多命令，运行时不能重复扫描。建议采样：

- `x = -18,-12,-6,0,6,12,18`
- `z = -6,2,10,18,26,34,42,50,58`
- 共 63 个基础样本。
- Boss 圆心 `^0 ^ ^44` 周围再检查 `x/z = -12,0,12` 的九点五格净空。
- 所有实际生成敌人的脚点（五名罪仆、Boss、米拉）再做一遍精确三点检查；任一失败都拒绝启动，不尝试“就近修地”。

地面判定至少排除：三种空气、水、熔岩、细雪、火、灵魂火、仙人掌、岩浆块、营火、灵魂营火、甜浆果丛、凋零玫瑰和尖滴水石。头部空间应严格为空气；宁可拒绝长草地，也不要静默清草。

### 3.3 失败回退

校验使用临时标签 `rpg.ch1.probe`，不得预先给玩家永久完成标签或物品。失败时：

- 删除 `rpg.ch1.probe`。
- 不创建 Bossbar，不写 `rpg_ch1_stage`，不标记队员。
- 告诉发起者第一个失败类别：`维度错误／场地未加载／地面不平／净空不足／附近已有村民或战斗／章节占用中`。
- 给出建议尺寸“宽 37、前方长 58、后方留 6、净空 5”。

严禁使用 `/fill`、`/setblock`、`/clone`、`/place`、`/forceload` 作为自动修复。

---

## 4. 多人资格与章节所有权

### 4.1 固定成员与临时在场者分离

沿用现有标签但纠正语义：

- `rpg.ch1.host`：发起者，仅一人。
- `rpg.ch1.member`：本次固定成员名单。
- `rpg.ch1.party`：固定成员中当前在锚点 96 格内、非旁观且在线者；只用于即时 HUD、对白和战斗判定。

启动后的 10 秒集结倒计时内，把 16 格内非旁观玩家标记为 `member`，并在聊天栏明确提示“留在范围内即加入本次档案”。倒计时结束后锁定名单；第二幕罪仆生成后不接受迟到成员。重演同样重新锁定名单。

当前生成器每刻清空 `party` 后把 96 格内的所有玩家加入，这是不合格实现：路人可以中途获得器具、触发线索和领取首通奖励。必须改成：

```mcfunction
tag @a[tag=rpg.ch1.party] remove rpg.ch1.party
tag @a[tag=rpg.ch1.member,distance=..96,gamemode=!spectator] add rpg.ch1.party
```

所有调查点、物资箱、裁决按钮和尾声救援都要求执行者同时具有 `rpg.ch1.member`；只有 `party` 接收现场演出。

### 4.2 暂停规则

- `unless entity @a[tag=rpg.ch1.member,distance=..96,gamemode=!spectator]` 时暂停剧情计时、粒子和刷怪。
- 暂停不删除 NPC、罪仆、Boss、仪式或检查点。
- Boss 通用十分钟寿命也必须随章节暂停：没有在场成员时给 `rpg.ch1.boss` 每刻补回 1 点 `rpg_fall`，上限 12000；否则玩家离线会把 Boss 寿命白白耗完。
- 任一成员返回 96 格内后恢复。
- 非成员进入区域不会恢复章节，也不能推动目标。

### 4.3 奖励资格

章节完成只结算当时在线且属于 `member` 的玩家。离线成员不被标记完成，也不凭空获得物品；其档案保留“未结案”，可另行重演。这样不需要伪造离线玩家选择，也不会丢失首通机会。

---

## 5. 状态、标签和检查点接口

### 5.1 现有计分板继续使用

- `rpg_ch1_stage`：大幕状态，0–9。
- `rpg_ch1_time`：当前幕有效运行刻数；暂停时不增加。
- `rpg_ch1_obj`：当前幕可显示数量，不作为唯一事实源。
- `rpg_ch1_choice`：1 消灭、2 放逐、3 封印、4 契约。
- `rpg_ch1_done`、`rpg_ch1_next`、`rpg_ch1_replay`：玩家永久档案。

### 5.2 需要新增的锚点计分板

- `rpg_ch1_sub`：幕内子阶段／波次。
- `rpg_ch1_empty`：无有效成员或团灭计时。
- `rpg_ch1_retry`：当前章恢复次数，用于首败完整对白、后续短提示。
- `rpg_ch1_safe`：只在启动预检中计数，正式启动后归零。
- `rpg_ch1_guard`：米拉失守累计刻数。
- `rpg_ch1_roster`：锁定成员人数，1–4；5 人以上按 4 人平衡。

### 5.3 检查点事实使用锚点标签

不要只存一个累计数字。每个一次性调查点在锚点留下独立标签；场景实体被命令误杀或服务器重启后，可以判断应重建哪一个：

- `rpg.ch1.done.anom.ledger`
- `rpg.ch1.done.anom.mother`
- `rpg.ch1.done.anom.ash`
- `rpg.ch1.done.trail.1..4`
- `rpg.ch1.done.hyp.1..3`
- `rpg.ch1.done.cache.1..3`
- `rpg.ch1.witness.skill.1..5`
- `rpg.ch1.witness.ready`
- `rpg.ch1.done.rescue`

每幕 `rpg_ch1_obj` 由这些标签重算或只作显示缓存。一次性 point 函数必须先给锚点加标签，再播放演出、发放物品、删除 point；重复调用首先 `return 0`。

### 5.4 通用实体标签

- `rpg.ch1.scene`：所有可以在章节结束时移除的临时实体。
- `rpg.ch1.scene.s0..s9`：按幕清理，防止陈设累积。
- `rpg.ch1.actor`：剧情演员。
- `rpg.ch1.actor.eliya`、`rpg.ch1.actor.mira`、`rpg.ch1.actor.cassian`、`rpg.ch1.actor.severa`。
- `rpg.ch1.vacant.safe`：章节受控空缺者；必须从通用蔓延候选中排除。
- `rpg.ch1.minion`＋`rpg.ch1.wave.1..3`：章节罪仆与波次。
- `rpg.ch1.boss`：章节别西卜。
- `rpg.ch1.point.*`、`rpg.ch1.label.*`、`rpg.ch1.prop.*`：交互点、标签、纯陈设。

章节 `clear_stage` 只能清指定 `scene.sN`，不能把仍参与下一幕的米拉／伊莱亚清掉；`clear_all` 在锚点 72 格范围内清 `scene`、章节罪仆和 Boss，随后删除锚点。所有演员必须预先 `rpg.vac.seen`，避免通用空缺者随机标记器把剧情 NPC 随机污染。

---

## 6. 分幕可玩流程

## 6.0 序幕：第十三声钟（1.5–2.5 分钟）

### 布置

- 锚点原点为封锁线。
- 伊莱亚：`^-2 ^ ^1`，村民或无 AI 的教廷书记员演员。
- 三只空碗 item display：`^5 ^ ^5`、`^-5 ^ ^7`、`^1 ^ ^12`，只做环境引导。
- 用 13 次钟声节拍演出，但第 1–12 声间隔 6 刻，第 13 声延迟 24 刻并立刻压低环境声；总演出约 6 秒，不让玩家站着等一分钟。

### 可玩目标

1. 玩家在集结倒计时内组成成员名单。
2. 靠近伊莱亚 3 格触发对话；对话三段每段 35–45 刻。
3. 对话完成才进入后街，避免纯计时自动跳幕。

### 检查点

锚点创建成功即 CP0。若世界重载，恢复伊莱亚和简报按钮；不会重复锁定新成员。

## 6.1 第一幕：会回家的死者（4–6 分钟）

### 异常 A：明日死亡名册——先取得辨认手段

坐标 `^-7 ^ ^10`。使用书／纸 item display、封蜡粒子和 interaction 或 2.5 格近距触发。

完成后：

- 播放玩家名字出现在明日处决名单上的文本。
- 伊莱亚打开应急箱。
- 每位 `member` 仅在背包没有 `totem_tag:1b` 时获得一件标准驱魔图腾；不要发完整创造测试箱。
- 锚点加 `rpg.ch1.done.anom.ledger`。
- 提示“把圣器握在手中，靠近仍在切面包的人”。

### 异常 B：没有倒影的餐桌——真实空缺者教学

场景：

- 空缺者母亲：`^6 ^ ^8`，村民，`NoAI:1b,Invulnerable:1b,PersistenceRequired:1b`，标签 `rpg.ch1.actor,rpg.ch1.vacant.safe,rpg.vac.seen,rpg.vacant`。
- 孩子：`^3.5 ^ ^8`，幼年村民，`Invulnerable:1b,NoAI:1b,rpg.vac.seen`，不带 `rpg.vacant`。
- 餐桌只用 1 个 block display 桌面、7 个碗／面包 item display；无碰撞。
- 母亲初始 `rpg_vac_x=-100`，避免圣器刚进入 16 格就直接撕壳。

玩法：成员手持／穿戴圣器，通用空缺者系统会让母亲发光并产生幽匿魂粒子。累计照见约 20 刻、`rpg_vac_x>=-80` 后，章节函数确认“辨认成功”：

1. 锚点加 `rpg.ch1.done.anom.mother`。
2. 调用安全净化或至少移除母亲的 `rpg.vacant`、`rpg.vac.torn`，归零 `rpg_vac_x`，清 6 格内章节空壳碎片。
3. 母亲继续以空缺演员存在，重复切七份面包；不要杀死她。
4. 给成员一次性提示“攻击空壳只会让污染转移；本章要求见证，不要求处决”。

共享系统必须把 `rpg.ch1.vacant.safe` 从 `vacant/spread` 随机候选中排除，否则母亲可能把剧情孩子或附近村民感染。母亲无敌，避免玩家误杀触发 `vacant/transfer` 随机召来另一位领主。

### 异常 C：第十三声钟灰

坐标 `^0 ^ ^16`。钟灰由火药／灰色染料 item display 与少量透明虫翅组成。调查后米拉在 `^2 ^ ^18` 出现，交出带血配给页，锚点加 `rpg.ch1.done.anom.ash`。

### 转场条件

三标签齐全且米拉完整说完“仓库从来不是空的，空的是人”后进入第二幕。不能只判断 `rpg_ch1_obj=3`。

## 6.2 第二幕：五席未满（7–10 分钟）

### 战斗开场

米拉退到 `^0 ^ ^17`，伊莱亚退到 `^-5 ^ ^17`。两人都是章节演员和 `rpg.vac.seen`；不参与伤害，但米拉进入护卫压力判定。

每轮开始前 35 刻给职责提示，敌人再落地。所有生成都使用现有函数并立刻在同一脚点选最近、同 lord/role 的新实体加章节标签：

```text
rpg:minion/summon/beelzebub/zepar  → lord4 role1 → wave1
rpg:minion/summon/beelzebub/botis  → lord4 role2 → wave1
rpg:minion/summon/beelzebub/bathin → lord4 role3 → wave2
rpg:minion/summon/beelzebub/sallos → lord4 role4 → wave2
rpg:minion/summon/beelzebub/purson → lord4 role5 → wave3
```

### 第一轮：封路与追猎

- 桀派 `^8 ^ ^24`；布提斯 `^-8 ^ ^25`。
- 战术目标：桀派近战封路，布提斯远射追击；UI 提示先断开二者夹击，不规定唯一击杀顺序。
- 两者全部死亡后，自动记录“谷壳城防通行证／见证人追猎名单”，但不生成可刷取的实体物品。

### 第二轮：转运与伪记忆

- 巴钦 `^9 ^ ^29`；塞列欧斯 `^-9 ^ ^29`。
- 巴钦现有司祭为 `NoAI`，位置必须与玩家有无遮挡视线，不能刷进装饰货架。
- 塞列欧斯施加幻觉时播放一条虚假家庭对白；效果结束后伊莱亚揭穿。
- 两者死亡后记录“尸体转运单／圣歌改写页”，显示卡西安印章。

### 第三轮：处刑者

- 布松 `^0 ^ ^32`，单独登场。
- 现有处刑者 108 HP、11 攻击；用 30 刻粒子线明确处决蓄势，不增加无提示秒杀。
- 击败后记录“第七粮仓封印钥／半张判词”。

### 多人强度

五种职责各只出现一次，保持所罗门名单的叙事唯一性。根据锁定 `rpg_ch1_roster` 调整章节罪仆最大生命与当前生命，不复制同名罪仆：

| 人数 | 生命倍率 | 伤害调整 |
|---:|---:|---:|
| 1 | 1.00 | 原值 |
| 2 | 1.25 | 原值 |
| 3 | 1.50 | 原值 |
| 4+ | 1.75 | 原值 |

Boss 始终 700 HP，不套这个倍率。

### 米拉护卫与捕获

不是传统易死护送：米拉保持无敌，避免固定结局被一次箭矢破坏；但当任一章节罪仆在她 8 格内、且 10 格内没有活着的成员时，锚点 `rpg_ch1_guard` 每刻增加。累计 120 刻后：

1. 米拉被传送到 `^0 ^ ^35` 的“待登记者”标记点，进入 `rpg.ch1.mira.captured`。
2. HUD 变为“在 03:00 内救回米拉”。
3. 成员靠近她 3 格并保持 40 刻即可解除；不需要杀友军或破坏方块。
4. 3600 刻超时，清整组五名章节罪仆，回 CP1，待成员回到封锁线后从第一轮重置。

### 团灭

当所有在场成员生命为 0，或所有成员离开 96 格且场上仍有章节罪仆，`rpg_ch1_empty` 开始计时。持续 200 刻：

- 杀掉锚点 72 格内的 `rpg.ch1.minion`。
- 清空 `sub`、`guard`，恢复米拉位置。
- 给锚点 `rpg.ch1.retry.pending`；下一名成员回来时从第一轮重生五席。
- 三处异常、玩家真名永久档案和本章身份状态保留。

剧情定稿要求“整组重置”，所以即使已经打到布松，团灭仍从桀派／布提斯开始。

## 6.3 第三幕：粮仓没有粮食（3–5 分钟）

四条腐蝇痕迹必须顺序出现，不能像当前实现一样全部同时摆出并允许乱序踩点：

1. 空口粮袋：`^-4 ^ ^32`
2. 带血车辙：`^5 ^ ^37`
3. 只剩姓名的配给牌：`^-5 ^ ^42`
4. 堆在封条外的空碗：`^0 ^ ^48`

每完成一处，锚点写 `done.trail.N`，清该点并生成下一点；米拉在安全距离传送到上一处痕迹后方，形成护送感而不运行昂贵寻路。第四点完成后生成卡西安演员 `^0 ^ ^50`，播放他否认伊莱亚姐姐存在的完整对话；对话结束才确认第七粮仓为活动区域。

若米拉在第二幕已被捕获，不能进入本幕，必须先救回。

## 6.4 第四幕：真名不在嘴里（5–7 分钟）

三处环境罪证围绕粮仓圆心 `^0 ^ ^44`：

- 灰烬中的余餐：`^-8 ^ ^40`
- 胃囊般的蝇茧：`^8 ^ ^42`
- 拒食的腐败祭品：`^0 ^ ^51`

三处可任意顺序调查，各有 2–3 段观察节拍和一次近距粒子重构；这样玩家在看证物，不是在跑三个发光文字点。

### P0：环境假说与正式真名必须拆开

当前 `add_beelzebub_campaign.py` 在三处 point 中直接调用：

```text
rpg:inquest/clue/4_1
rpg:inquest/clue/4_2
rpg:inquest/clue/4_3
```

通用 `recount/4` 在任意三条 `rpg.clue.4.*` 后立刻调用 `reveal/4`，授予 `rpg.name.4` 和真名残页。因此当前实现会在 Boss 出现前“快进到确定真名”，直接违背剧情定稿。

关卡正确接口：

- 环境点只给锚点 `rpg.ch1.done.hyp.1..3`，不调用任何 `inquest/clue/4_*`。
- 三点齐全时只显示“真名假说：别西卜；尚缺三项权能见证”和弱点“腐败残食”。
- 成员可以得到弱点媒介，但不能得到已确证真名标签。
- 正式 `rpg.name.4` 留到第六幕三种 Boss 招式见证后统一授予。

已在其他玩法中获得 `rpg.name.4` 的老玩家也不能跳过本章见证；章节绑定门禁只看锚点 `rpg.ch1.witness.ready`，不只看玩家永久真名标签。

## 6.5 第五幕：被撕去的判词（3–4 分钟）

物资点：

1. 封印档案箱 `^-10 ^ ^45`：章节专用“待确证残页”＋腐宴残食媒介。
2. 圣器保管箱 `^10 ^ ^45`：若缺失则补驱魔图腾；浓缩圣水、银质圣钉。
3. 裁决器具箱 `^0 ^ ^53`：告解铃、净罪香、一种由玩家选择的粉笔、封魔灯。

### 领取规则

- 箱子按锚点标签一次性开启，但器具按每位 `member` 的物品检测补发，防止一个人先开箱导致其他固定成员没有工具。
- 不要给每人三种粉笔；让每位成员从守御／压制／疾行中选一种，顺便预演职业路线。
- “待确证残页”使用独立 `custom_data={rpg_ch1_pending_page:1b}`，不能触发通用法阵右键。
- 第六幕见证完成时清掉待确证残页并调用标准 `rpg:inquest/give/page4`。
- 重试 Boss 时仅补齐缺失的章节必需品，不整套重复发放。

玩家检查判词后必须看到页码 7 → 9 的缺口；卡西安的“第八页只是过时格式”完整播放后才能进入 Boss。

## 6.6 第六幕：万蝇腐宴（12–16 分钟，含四阶段）

### 登场

- Boss 圆心固定 `^0 ^ ^44`。
- 卡西安演员先念三项配给数字，随后清演员并在同一点调用 `rpg:taint/lord4`。
- 立刻给最近、`rpg_dm_lord=4` 的新卫道士加 `rpg.ch1.boss`。
- 校验 `Health=700f`、最大生命 700、`rpg_ex_stage=0`、`rpg_fall=12000`。
- Boss 生成失败时不推进对白；80 刻内重试一次，再失败则回第五幕结束点并给管理员诊断，而不是每刻无限召唤。

### 驱魔一：镇压＋三种权能见证

通用五招已经在 `taint/sk4_1..5` 中调用 `inquest/clue/4_1..5`。章节需要在同一五个技能入口追加一条只对本章 Boss 生效的见证分发：

```text
Boss cast sk4_N
  → 对 18 格内、rpg.holy、rpg.ch1.party 的成员调用
    rpg:campaign/beelzebub/witness/skillN
  → 给锚点写 rpg.ch1.witness.skill.N
  → 重算不同见证数
```

三种不同技能齐全后：

1. 锚点加 `rpg.ch1.witness.ready`。
2. 对所有本章 `member` 调用一次 `rpg:inquest/reveal/4`（已有永久真名者只同步章节状态，不重复 XP／物品）。
3. 将待确证残页替换为标准真名残页。
4. 播放“现实承认祂来过”。

如果 Boss 已到 420 而见证不足三种，保持通用锁血，继续允许其施放未见证技能。技能抽取本身不连续重复，五招中取三通常在 4–6 次施法内完成。必要时 90 秒仍不足三证，章节可优先从未见证技能中选一招，但不能直接赠送见证。

### 驱魔二：镇魔门禁

通用 `inquest/stage1` 对别西卜的绑定条件必须为：

- 非章节 Boss：保持原通用条件。
- `rpg.ch1.boss`：除玩家 `rpg.name.4` 和点燃图腾外，额外要求附近章节锚点有 `rpg.ch1.witness.ready`。

这能防止老玩家带着既有真名直接点燃图腾跳过本章调查。

### 驱魔三：固阵

完整复用通用法阵、稳定度、暴食吞媒介、银钉、铃、香与粉笔。关卡只做叙事桥接：

- 稳定度首次到 70：米拉“名字在回来”。
- 首次到 85：伊莱亚发现缺页。
- 首次到 95：别西卜点破教廷留刀。
- 首次到 100：卡西安残声宣布许可有效。

四个阈值各用锚点标签去重，不能因稳定度上下波动反复刷对白。

法阵阶段允许现有罪仆生态按压力轮换召唤，但章节场上罪仆总数上限 3；超过上限时跳过本轮召唤，避免 Boss＋粒子＋五罪仆造成卡顿。

### 驱魔四：裁决

通用四按钮照常出现。章节专属结算在通用 outcome 顶部拦截 `rpg.ch1.boss + rpg.exorcism.bound`，记录选择后进入第七幕。场景层不自行假造裁决按钮。

### Boss 异常恢复

- Boss 离场、被命令删除、寿命归零：保存锚点环境假说、技能见证、物资箱与永久真名；清活动仪式，回 Boss 登场检查点。
- 首次完整说明卡西安空壳崩坏；后续只给短提示。
- 不重复给标准真名 XP、物资或残页。
- 稳定度归零：拦截通用“强制消灭完整核心”结果，清仪式、把章节 Boss 恢复到镇魔前状态；保留 `witness.ready`，要求重新点燃图腾。

## 6.7 第七幕：四种不完整的裁决（2–4 分钟）

场景层只负责空间与节拍：

- 消灭：空壳裂开，掉出的仅为粮票／虫翅 display。
- 放逐：短暂裂隙向内吸，随后虫群逆飞。
- 封印：封魔灯 display 变黄绿，播放卡西安重复报数。
- 契约：书页出现胃形印，随后自动断开。

共同段在裁决专属段之后播放：缺页灰烬 → 未登记姓名 → 万蝇吞名 → 通风井逃脱。Boss 删除必须走章节 escape，避免乘客死亡探针触发通用完整掉落。

裁决超时不能替玩家选路线。超时只播放一次“空壳试图沿放逐缺口逃逸”，随后恢复四按钮；没有主动选择就没有残响。

## 6.8 尾声：活着的人必须有名字（5–7 分钟）

清掉粮仓战斗陈设，恢复封锁线舞台：

- 米拉 `^1 ^ ^9`，濒死但无敌。
- 塞维拉 `^-3 ^ ^7`。
- 两名无名教廷士兵 `^-7/+7 ^ ^7`。
- 伊莱亚 `^-1 ^ ^12`，随后被带走。

米拉四句个人陈述按 45 刻间隔完整播放后，才开放“释放魔力”按钮。按钮执行者必须是 `member` 且在米拉 12 格内；只允许第一次成功者推动锚点，演出作用于全队。离开现场时章节暂停，返回后从塞维拉处决令前恢复，不能自动救人。

救援后进入职业登记。首通必须完成一次审判／守护／秘仪选择才算结章；若主生成器暂时还没有三路线实际技能，至少不能假装“已强化”，应停在登记页面等待实现。完成后清场、发首通身份与对应残响、开放路西法预调查入口。

---

## 7. 运行时预算与性能约束

### 7.1 实体预算

| 阶段 | 剧情演员 | 敌对实体 | marker / interaction | display | 建议总数 |
|---|---:|---:|---:|---:|---:|
| 后街调查 | 4 | 0 | 3–4 | ≤12 | ≤20 |
| 罪仆战 | 2 | 1–2 | 2 | ≤8 | ≤14 |
| 追踪／环境调查 | 3 | 0 | ≤4 | ≤14 | ≤21 |
| Boss 战 | 2 | Boss 1＋罪仆≤3 | 通用仪式实体 | 场景≤10 | 全章额外≤18 |
| 尾声 | 5 | 0 | 1 | ≤6 | ≤12 |

每次换幕先清前幕纯陈设。禁止把第一幕餐桌、第二幕全部标签和第四幕证物一直留到 Boss 战。

### 7.2 Tick 预算

- 场景状态机只在活动锚点存在时执行。
- 距离／点位检测每 5 刻一次；粒子脉冲每 10 刻一次；纯环境音每 40–80 刻一次。
- 只有 Boss 战和短演出允许 force 粒子；普通调查粒子用 normal。
- 单次环境脉冲 ≤12 粒子；单次关键技能演出可到 60；逃脱总量可到 180，但拆成 3–4 个时序脉冲，不同刻发出。
- 不在每刻对所有 `@e` 做无类型全局扫描。优先 `type + tag + distance + limit`。
- 章节罪仆能力继续使用现有十刻节拍，不另加一套每刻 AI。
- 文本 display 的 `view_range` 只覆盖局部，离开一幕后删除。

### 7.3 低配降级

可增加玩家选项 `简化章节特效`，只影响场景陈设和粒子，不影响技能预警、法阵边界、真名见证和危险区。不能为了性能关闭可读性必要的 Boss Telegraph。

---

## 8. 主生成器可直接采用的函数契约

以下名称是建议的稳定接口；关卡逻辑不要散落在 `stage/N_tick` 的长命令列表中：

```text
campaign/beelzebub/scene/preflight
campaign/beelzebub/scene/preflight_sample
campaign/beelzebub/scene/preflight_pass
campaign/beelzebub/scene/preflight_fail
campaign/beelzebub/scene/snap_rotation
campaign/beelzebub/scene/clear_stage
campaign/beelzebub/scene/clear_all

campaign/beelzebub/roster/open
campaign/beelzebub/roster/lock
campaign/beelzebub/roster/sync_presence

campaign/beelzebub/anomaly/ledger
campaign/beelzebub/anomaly/mother_seen
campaign/beelzebub/anomaly/ash

campaign/beelzebub/minion/spawn_wave1
campaign/beelzebub/minion/spawn_wave2
campaign/beelzebub/minion/spawn_wave3
campaign/beelzebub/minion/scale
campaign/beelzebub/minion/wipe
campaign/beelzebub/mira/capture
campaign/beelzebub/mira/rescue_capture

campaign/beelzebub/trail/1..4
campaign/beelzebub/hypothesis/1..3
campaign/beelzebub/cache/1..3
campaign/beelzebub/cache/reissue_missing

campaign/beelzebub/witness/skill1..5
campaign/beelzebub/witness/recount
campaign/beelzebub/witness/confirm
campaign/beelzebub/boss/recover
campaign/beelzebub/rite/collapse
```

执行者约定：

- `scene/*`、`stage/*`、`witness/recount`、`boss/recover`：`@s` 是章节锚点，执行位置在锚点。
- `anomaly/*`、`trail/*`、`hypothesis/*`：`@s` 是 point marker，函数必须在附近找到唯一锚点并先写完成标签。
- `witness/skillN`：`@s` 是见证技能的玩家；函数只写附近唯一活动锚点，玩家必须为 `member + party + holy`。
- `minion/scale`：`@s` 是刚生成且已带 `rpg.ch1.minion` 的罪仆。
- 所有函数可重复调用而不重复奖励，幂等性由锚点 done 标签和玩家物品检测保证。

---

## 9. 当前 `add_beelzebub_campaign.py` 与目标差距

| 优先级 | 当前实现 | 问题 | 必须调整 |
|---|---|---|---|
| P0 | 第四幕直接调用 `inquest/clue/4_1..3` | Boss 前立刻正式真名确证 | 改为章节环境假说标签；Boss 三招再确证 |
| P0 | `party` 每刻吸收 96 格内所有玩家 | 路人可触发、领工具、领首通奖励 | 新增固定 `member`，party 仅为成员在场子集 |
| P0 | 五名罪仆同时生成 | 单人峰值过高、职责和三段剧情消失 | 改三波 2／2／1，整组团灭重置 |
| P0 | 第一幕只有三个 marker＋label | 没有真实空缺者玩法、米拉／伊莱亚无演员 | 加受控真实空缺者母亲、孩子与演员 |
| P0 | Boss 绑定只看通用 `rpg.name.4` | 老玩家或错误环境确证可跳过章节见证 | 章节 Boss 额外要求 `witness.ready` |
| P1 | 启动不检查维度、地面、净空或附近村民 | 可刷进墙、液体、玩家村庄 | 启动前 63＋9 点预检，失败零残留 |
| P1 | 追踪四点同时生成 | 可乱序踩完，追踪没有路径感 | 顺序生成四点 |
| P1 | 物资箱一次给所有当前 party | 迟到／暂离成员漏领，重试可能重复 | 固定成员逐人检测缺失组件再补发 |
| P1 | 只有 boss missing 自动重生 | 团灭、米拉捕获、仪式归零、裁决超时未落地 | 增加检查点恢复状态 |
| P1 | 结束给每位成员裁决残响，重演也会给 | 与“实体残响首通一次”冲突 | 重演只记档案，不重复实体残响 |
| P1 | 阶段 9 直接完成，没有职业三选一 | 缺少“强化职业／圣物／契约”实际步骤 | 职业选择完成后才能结章 |
| P2 | 序幕 150 刻自动跳过 | 叙事只有 7.5 秒，不是可玩序幕 | 改成靠近伊莱亚＋对话完成门禁 |
| P2 | 全局 `kill @e[tag=rpg.ch1.scene]` 等 | 单实例时仍可能误删远处残留／调试实体 | 所有清理锚点定位并限 72 格 |
| P2 | 逃脱一刻发送 310+ 粒子 | 低配客户端帧尖峰 | 拆 3–4 刻脉冲并控制总量 |

---

## 10. 验收矩阵

### 10.1 场地与无破坏

- 在超平坦主世界启动：通过，朝向吸附，所有脚点落地。
- 在斜坡、树林、低顶棚、水面、下界、玩家村庄旁启动：拒绝，零场景残留。
- 完整通关前后对比区域方块：没有任何方块、方块实体或容器内容变化。
- `/function rpg:campaign/beelzebub/abort`：只清当前锚点 72 格内章节实体，不影响普通村民、普通恶魔与玩家建筑。

### 10.2 单人与多人

- 单人：三波同时敌人不超过 2，45–55 分钟可完成。
- 2／4 人：成员锁定后生命倍率正确，Boss 仍为 700；每个成员都能拿到必需器具。
- 路人中途进入：看不到交互按钮，不能推动 point，不能领器具／奖励。
- 固定成员掉线：章节暂停或由其他成员继续；返回后仍是 member。
- 所有成员离开：剧情和 Boss 寿命暂停，不在无人区继续刷粒子。

### 10.3 空缺者

- 母亲保有姓名／外观／台词，不持圣器时没有显形特效。
- 成员取得圣器后靠近，母亲由通用空缺者机制发光；约 1 秒完成辨认，随后安全移除污染状态。
- 母亲不会参与通用蔓延，不会感染孩子或附近玩家村民。
- 玩家攻击无法杀死她，也不会触发随机领主降临。

### 10.4 调查与真名

- 三个环境罪证完成后：有假说和弱点，没有 `rpg.name.4`、没有通用已确证残页。
- Boss 战见证同一技能三次：只计 1。
- 见证三种不同技能：锚点 `witness.ready`，成员正式确证真名，待确证残页替换。
- 老玩家预先有 `rpg.name.4`：仍不能在章节三证前绑定 Boss。

### 10.5 战斗与恢复

- 三波顺序、职责、线索和卡西安印章全部出现。
- 任意波团灭：五名整组重置，三异常不重做。
- 米拉失守：进入三分钟救援；救回继续；超时回街战检查点。
- Boss 被 `/kill`、离开区域或寿命结束：回 Boss 登场，环境调查／见证／领取状态保留且不刷奖励。
- 稳定度归零：不掉完整核心；保留真名，要求重新镇魔。
- 裁决超时：不自动选择，不给残响，恢复选择。

### 10.6 结局

- 四裁决各自演出后都进入共同逃脱；原因明确是见证人印被教廷删去。
- 无任何路线掉落完整别西卜核心、罪器、封印遗物或真实契约。
- 米拉完整说出姓名和个人记忆后，必须由玩家主动救援。
- 首通必须完成一次职业路线选择，才授予入院令、首通阅历、单个裁决残响和路西法预调查入口。
- 重演可记录不同裁决，但不重复入院令、60 点阅历、职业强化或实体残响。

---

## 11. 最小可交付顺序

为避免先堆美术再推翻逻辑，主代理应按以下顺序整合：

1. 修正 P0 真名拆分与章节 Boss 绑定门禁。
2. 加安全预检、正交锚点与固定成员名单。
3. 把第一幕改为真实空缺者演员与圣器辨认。
4. 把五罪仆改为三波并完成团灭／米拉捕获恢复。
5. 顺序追踪、环境假说、逐人补发物资。
6. 接入 Boss 三招见证、仪式崩溃和 Boss 异常恢复。
7. 补职业三选一与重演奖励门禁。
8. 最后按 `_campaign_beelzebub_art_ui.md` 替换 display、粒子、音效与文本节拍；视觉层不得改变本文件的状态接口。

这套顺序完成后，第一章才从“会自动推进的系统演示”变成一段完整、可失败、可恢复、能承载剧情因果的 30–60 分钟 RPG 战役。
